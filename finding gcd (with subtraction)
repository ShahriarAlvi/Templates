#include <bits/stdc++.h>

using namespace std;

int gcd(int m, int n)
{
    while(m != n)
    {
        if(m > n)
            m = m - n;
        else
            n = n - m;
    }

    return m;
}

int main ()
{
    int m,n;
    cin >> m >> n;
    
    cout << gcd(m , n) << endl;
}
